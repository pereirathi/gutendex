import styled from '@emotion/styled'
import { theme } from '../../styles/theme'

export const WrapperInfiniteScroll = styled.div`
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  justify-content: space-evenly;
  margin: ${theme.sizes.Space2X} ${theme.sizes.Space2X};
`
